/**
 * Temporary manual order. The optimal cache friendly order should be derived from the order of the system execution.
 */
enum Cells
{
    node,
    transform,
    sprite,
    on,
    clockclock,
    digit,
    clockclock_ui,
    clock,
}

export default Cells
